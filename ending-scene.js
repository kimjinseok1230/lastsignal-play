import {CLASSES} from './data.js?v=cat20';
export function endingScene(data,step,finals,title){const cat=CLASSES.find(c=>c.id===data.classId)||CLASSES[0];
 const scenes=[['야간점장, 전원 OFF','시끄럽던 편의점이 조용해졌어요. 고양이들이 간식 창고를 무사히 지켜냈습니다.'],['진열 완료. 간식도 확보!','창고 3곳의 간식을 챙기고, 어질러진 매장을 정리했어요. 오늘 밤도 모두의 힘으로 해냈습니다.'],['오늘의 야간조, 무사 퇴근!','이제 따뜻한 방석과 간식이 기다리고 있어요. 수고했어, '+cat.name+'!']];const scene=scenes[step];
 return `<div class="eyebrow">무사 퇴근 · ${step+1} / 3</div><h2 id="modal-title">${scene[0]}</h2><div class="ending-scene"><img class="ending-store" src="./assets/cat-store.webp" alt="지켜낸 고양이 편의점"><div class="ending-crew">${[...new Set([data.classId,'runner','engineer','warden'])].slice(0,3).map(id=>`<img src="./assets/cat-${id}.webp" alt="${CLASSES.find(c=>c.id===id).name}">`).join('')}</div></div><p class="modal-intro">${scene[1]}</p>${step===2?`<div class="ending-honor"><small>이번 근무의 칭호</small><strong>${title}</strong><p>창고 ${data.relays}/3 · 보스 ${data.bossKills}기 · ${data.kills}마리 격파<br>최종 진화 ${finals}종 · 최대 ${data.bestCombo||0}콤보</p><b>간식 코인 +${data.credits} · 부활권 +1</b></div>`:''}<div class="modal-bottom"><button class="primary-button" data-action="ending-next">${step===2?'근무 성과 보기':'다음 이야기'}</button><button class="quiet-button" data-action="ending-results">연출 건너뛰기</button></div>`;
}
