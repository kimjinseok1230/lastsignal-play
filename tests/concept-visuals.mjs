import {test} from 'node:test';import assert from 'node:assert/strict';import {conceptVisuals} from '../concept-visuals.js?v=cat21';
test('concept props draw in both detail modes without changing combat state',()=>{for(const particles of [true,false]){let commands=0;const c=new Proxy({},{get:()=> (...args)=>{commands++;for(const a of args)if(typeof a==='number')assert(Number.isFinite(a));},set:()=>true});const g={visible:()=>true,settings:{particles},player:{x:0,y:0,maxHp:100},skillBuff:{kind:'guard',shield:45,flash:.1},u:{field:1},evolved:{absolute:true},bullets:[{type:'rocket',x:0,y:0,vx:1,vy:0},{catEffect:'stars',x:0,y:0}],skillObjects:[{kind:'clinic',x:0,y:0},{kind:'flame',x:0,y:0,angle:0}],beams:[{thread:true,x:0,y:0,tx:100,ty:0,life:.1},{bread:true,x:0,y:0,tx:100,ty:0,life:.1}],fx:[{canDebris:true,x:0,y:0,r:80,life:.2,max:.45}]};const before=JSON.stringify(g);for(const fn of Object.values(conceptVisuals))fn.call(g,c);assert(commands>100);assert.equal(JSON.stringify(g),before);}});
